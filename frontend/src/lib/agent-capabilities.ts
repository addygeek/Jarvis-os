/** @deprecated Import from `@/data/fallbackCapabilities` instead. */
export {
  FALLBACK_AGENT_CAPABILITIES as STATIC_AGENT_CAPABILITIES,
  QUICK_AGENT_EXAMPLES,
  countCapabilities,
  filterCapabilities,
} from "@/data/fallbackCapabilities";
